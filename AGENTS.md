# Agent Instructions

- Always commit and push changes directly to `main` (the default branch — there is no `master`). Do not create feature branches or pull requests unless explicitly asked.

See `CLAUDE.md` for the full project instructions.
